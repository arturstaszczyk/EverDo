{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "qmake",
            "type": "shell",
            "command": "mkdir -p build && cd build && /Users/arturstaszczyk/Qt/5.11.2/clang_64/bin/qmake /Users/arturstaszczyk/Projects/qt/EverDo/EverDo.pro -spec macx-clang CONFIG+=debug CONFIG+=x86_64 CONFIG+=qml_debug && /usr/bin/make qmake_all",
        },
        {
            "label": "make",
            "type": "shell",
            "command": "cd build && /usr/bin/make",
            "dependsOn": ["qmake"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "clean",
            "type": "shell",
            "command": "cd build && /usr/bin/make clean && cd .. && rm -rf build",
        }
    ]
}